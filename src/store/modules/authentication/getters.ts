import { GetterTree } from 'vuex';
import { RootState } from '@/store/types';
import { AuthState } from "@/store/modules/authentication/types";

export const getters: GetterTree<AuthState, RootState> = {

};
